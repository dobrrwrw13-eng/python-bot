# Инструкция по развертыванию BGPK Bot на GitHub

## 1️⃣ Подготовка файлов

### Убедитесь, что у вас есть:
- ✅ `.gitignore` - файл с игнорируемыми файлами
- ✅ `.env.example` - пример конфигурации БЕЗ паролей
- ✅ `requirements.txt` - список зависимостей
- ✅ `README.md` - документация

⚠️ **НЕ коммитьте:**
- `.env` файл
- `serviceAccountKey.json`
- `students.db`
- `announcement_files/`

## 2️⃣ Инициализация Git репозитория

```bash
cd /Users/imgioz/Desktop/Archive\ 2

# Инициализируем git
git init

# Добавляем все файлы (кроме игнорируемых)
git add .

# Проверяем что будет закоммичено
git status

# Коммитим
git commit -m "Initial commit: BGPK Telegram bot"
```

## 3️⃣ Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий с именем `bgpk-bot`
3. **НЕ инициализируйте** с README (у вас уже есть)
4. Скопируйте команды для push

## 4️⃣ Синхронизация с GitHub

```bash
# Добавляем удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/bgpk-bot.git

# Переименовываем ветку (если нужно)
git branch -M main

# Загружаем на GitHub
git push -u origin main
```

## 5️⃣ Настройка GitHub Secrets (для CI/CD в будущем)

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте secrets:
   - `TELEGRAM_BOT_TOKEN` - ваш токен бота
   - `EMAIL_PASSWORD` - пароль от почты
   - Другие чувствительные данные

## 6️⃣ Инструкция для других разработчиков

Создайте файл `DEPLOY.md`:

```markdown
# Как запустить проект локально

1. Клонируйте репозиторий:
   git clone https://github.com/YOUR_USERNAME/bgpk-bot.git
   cd bgpk-bot

2. Создайте виртуальное окружение:
   python3 -m venv venv
   source venv/bin/activate

3. Установите зависимости:
   pip install -r requirements.txt

4. Создайте .env файл из примера:
   cp .env.example .env

5. Заполните .env файл вашими данными:
   - TELEGRAM_BOT_TOKEN
   - EMAIL_PASSWORD
   - Другие ключи

6. Поместите serviceAccountKey.json в корень проекта

7. Запустите бот:
   python3 bot_bgpk.py
```

## 7️⃣ Примечания безопасности

### ✅ Что можно коммитить:
- Исходный код (bot_bgpk.py)
- Конфигурационные примеры (.env.example)
- Документацию
- requirements.txt

### ❌ Что НЕЛЬЗЯ коммитить:
- .env (реальные пароли)
- serviceAccountKey.json (Firebase credentials)
- students.db (база данных)
- Любые приватные ключи

### 🔐 Защита GitHub репозитория

1. Перейдите Settings → Code security and analysis
2. Включите "Secret scanning"
3. Включите "Dependabot alerts"

## 8️⃣ Обновление кода

Когда вы внесли изменения:

```bash
# Просмотр изменений
git status

# Добавьте нужные файлы
git add bot_bgpk.py README.md

# Коммит с описанием
git commit -m "Add news listener functionality"

# Отправка на GitHub
git push origin main
```

## 9️⃣ Загрузка на сервер (Linux)

```bash
# На сервере
ssh user@your-server.com

# Клонируем репозиторий
git clone https://github.com/YOUR_USERNAME/bgpk-bot.git
cd bgpk-bot

# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Создаем .env файл
nano .env

# Копируем Firebase ключ
# (скачиваете на компьютер, потом загружаете на сервер)
scp serviceAccountKey.json user@your-server.com:~/bgpk-bot/

# Запускаем бот в фоновом режиме
nohup python3 bot_bgpk.py > bot.log 2>&1 &

# Или используйте systemd (рекомендуется)
sudo nano /etc/systemd/system/bgpk-bot.service
# Вставьте содержимое из README.md
sudo systemctl enable bgpk-bot
sudo systemctl start bgpk-bot
```

## 🔟 Проверка безопасности

Используйте инструмент для проверки ошайчик:

```bash
# Проверка на случайно закоммиченные секреты
git log --all --full-history --source --remotes --oneline | head -20

# Или используйте git-secrets:
brew install git-secrets  # На macOS
git secrets --scan
```

## Готово! 🎉

Теперь ваш проект безопасно хранится на GitHub с защитой всех чувствительных данных!
